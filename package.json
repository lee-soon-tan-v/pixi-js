{
  "name": "my-pixi-game",
  "version": "1.0.0",
  "source": "src/index.html",                
  "scripts": {
    "start": "parcel --dist-dir dist",   
    "build": "parcel build --dist-dir dist"  
  },
  "dependencies": {
    "pixi.js": "^7.4.0"
  },
  "devDependencies": {
    "parcel": "^2.12.0"                   
  },
  "targets": {
    "default": {
      "distDir": "./dist"
    }
  }
}
